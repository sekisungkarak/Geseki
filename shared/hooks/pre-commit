#!/bin/sh
# Menjaga tag pratinjau tautan tiap shim docs selaras dengan entri katalognya,
# sehingga menambah widget tetap: salin shim, ubah id, commit.
node shared/tools/stamp-meta.mjs || exit 1
git add -u -- '*/docs/index.html'
